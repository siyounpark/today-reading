const K='today-reading-v1',q=id=>document.getElementById(id);let s=load(),draft='';function load(){try{return Object.assign({book:null,logs:[]},JSON.parse(localStorage.getItem(K)||'{}'))}catch{return{book:null,logs:[]}}}function save(){localStorage.setItem(K,JSON.stringify(s))}q('addBook').onclick=()=>openBook();q('editBook').onclick=()=>openBook(true);q('addLog').onclick=()=>{q('logTitle').textContent=s.book.title;q('logPage').value=s.book.page||0;let e=s.logs.find(x=>x.date===day(new Date()));q('note').value=e?.note||'';q('logDlg').showModal()};document.querySelectorAll('[data-close]').forEach(b=>b.onclick=()=>q(b.dataset.close).close());q('coverInput').onchange=e=>{let f=e.target.files[0];if(!f)return;let r=new FileReader;r.onload=()=>{let im=new Image;im.onload=()=>{let c=document.createElement('canvas'),z=Math.min(1,700/im.width);c.width=im.width*z;c.height=im.height*z;c.getContext('2d').drawImage(im,0,0,c.width,c.height);draft=c.toDataURL('image/jpeg',.75);q('preview').src=draft;q('preview').classList.remove('hide');q('previewText').classList.add('hide')};im.src=r.result};r.readAsDataURL(f)};function openBook(edit=false){let b=edit?s.book:null;q('titleInput').value=b?.title||'';q('authorInput').value=b?.author||'';q('pageInput').value=b?.page||0;q('totalInput').value=b?.total||'';draft=b?.cover||'';if(draft){q('preview').src=draft;q('preview').classList.remove('hide');q('previewText').classList.add('hide')}else{q('preview').classList.add('hide');q('previewText').classList.remove('hide')}q('bookDlg').showModal()}q('bookForm').onsubmit=e=>{e.preventDefault();s.book={title:q('titleInput').value.trim(),author:q('authorInput').value.trim(),page:+q('pageInput').value||0,total:+q('totalInput').value||0,cover:draft};save();q('bookDlg').close();render()};q('logForm').onsubmit=e=>{e.preventDefault();let p=+q('logPage').value||0;if(s.book.total)p=Math.min(p,s.book.total);s.book.page=p;let d=day(new Date()),x={date:d,page:p,note:q('note').value.trim(),title:s.book.title},i=s.logs.findIndex(v=>v.date===d);i>=0?s.logs[i]=x:s.logs.unshift(x);s.logs.sort((a,b)=>b.date.localeCompare(a.date));save();q('logDlg').close();render()};function day(d){return`${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,'0')}-${String(d.getDate()).padStart(2,'0')}`}function dfrom(x){let[a,b,c]=x.split('-').map(Number);return new Date(a,b-1,c)}function streak(){let a=[...new Set(s.logs.map(x=>x.date))].sort().reverse();if(!a.length)return 0;let t=new Date,y=new Date(t);y.setDate(t.getDate()-1);if(a[0]!==day(t)&&a[0]!==day(y))return 0;let set=new Set(a),c=dfrom(a[0]),n=0;while(set.has(day(c))){n++;c.setDate(c.getDate()-1)}return n}function esc(x){return String(x).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}function render(){let b=s.book;q('empty').classList.toggle('hide',!!b);q('reading').classList.toggle('hide',!b);q('streak').textContent=streak();if(b){q('title').textContent=b.title;q('author').textContent=b.author||'저자 미입력';q('page').textContent=b.page;q('total').textContent=b.total||'?';let p=b.total?Math.min(100,Math.round(b.page/b.total*100)):0;q('percent').textContent=p+'%';q('bar').style.width=p+'%';if(b.cover){q('cover').src=b.cover;q('cover').classList.remove('hide');q('coverEmpty').classList.add('hide')}else{q('cover').classList.add('hide');q('coverEmpty').classList.remove('hide')}}q('logs').innerHTML=s.logs.length?s.logs.slice(0,12).map(x=>`<article class="log"><div class="logtop"><b>${esc(x.title)}</b><span>${+x.date.slice(5,7)}월 ${+x.date.slice(8)}일</span></div><p><b>${x.page}쪽까지 읽음</b></p>${x.note?`<p>${esc(x.note)}</p>`:''}</article>`).join(''):'<p class="empty">아직 기록이 없어요.<br>오늘 한 페이지부터 시작해 보세요.</p>'}render();